# Feature Detail: Flink Workspace Capacity Allocation

**Feature ID**: 013\
**Name**: Flink Workspace Capacity Allocation\
**Description**: Separate paid Workspace lifecycle from authoritative PRE capacity, namespace, and implicit queue reconciliation.\
**Status**: Implemented; local integration and final whole-branch review complete, deployment pending\
**Created**: 2026-07-16\
**Last Updated**: 2026-07-20

## Overview

This feature introduces `alicloud_flink_workspace_capacity_allocation` as the single authoritative owner of a Workspace's fixed, cross-zone fixed, maximum capacity, complete namespace set, and each namespace's implicit `default-queue`. `alicloud_flink_workspace` remains the owner of purchase, billing, network, HA topology, create-time `initial_capacity`, import, and release. One Workspace may have only one allocation owner across Terraform states and processes; the provider process lock is not cross-state uniqueness.

## Latest Review

The provider integration contains the reviewed allocation resource, topology adapter, Workspace initial-capacity mode, and registry/coordinator cleanup. The reviewed cws-lib pin is `fa9c9495be328abea48ab38fb6cafbdd0983f1d3`: it serializes non-nil namespace `ResourceSpec`, preserves legacy nil-spec behavior, uses the explicit Workspace ID, and reports only a confirmed Create-success/post-read failure as typed `FlinkPostCreateReadError`. Any such typed error, whether its cause is 404 or non-404, denotes confirmed Create/post-read ambiguity; direct service/API/business rejections remain terminal, while ordinary `FlinkSDKError` transport failures retain the general ambiguous-write policy.

Phase 12 closed the two functional Major findings from the first whole-branch review. Legacy Workspace Create now waits only for the service-created default namespace and `default-queue` required by the current callback, without validating unrelated siblings or returning a post-ID observer error from the paid parent. The capacity adapter now requires every successful Workspace read to return a non-empty exact `InstanceId` before any namespace/queue read or capacity write. Both changes passed independent high-risk review with `0 Critical / 0 Major / 0 Minor`. The merged source is `c7b98d47a098374347e3284ce768a86a67f32ae4`; the final local provider binary is version `1.247.0`, SHA-256 `edeab3ae6ab09ad86ce64c2382f4693760374273445d8df4564c9a8de94aa0d2`.

OpenTofu `1.10.10` allocation-recovery and production-callback runtime gates pass. Three existing `cws-lib-iac` module/suite targets pass isolated-mirror init, installed-executable SHA verification, and validate. All ten active xuanji `21-flink` leaves produce baseline/candidate-equivalent normalized plans; each plan binary was deleted and each leaf lock was restored. The temporary live probe and its cloud dependencies were already cleaned in earlier phases, except for the explicitly accepted Resource Group `PendingDelete` control-plane tombstone. Canonical iac-env deployment and its OpenTofu `1.7.0-dev` verification remain a later, separately authorized deployment phase.

The final independent whole-branch review covered `593305ec51e8da5a9c83edc41f085c817a2d6237..dbc12fb0d2bbd54a244e6bb6d115e15999c8f955` (64 commits) and returned `APPROVED — 0 Critical / 0 Major / 0 Minor`. It explicitly allows local merge-back and cleanup while keeping canonical iac-env deployment forbidden pending separate authorization. The report is `/tmp/provider-phase12-final-whole-branch-review.md`, SHA-256 `faa812cbedc478db02628f1840fb30213b74e7d6bd4c8849f1fd9dae6015ad1b`.

## Key Changes

1. Allocation is PRE-only. Its first authoritative Read rejects POST before any cloud write. Workspace `initial_capacity` is an integer-CU, create-only input serialized only to `CreateInstance`; it is mutually exclusive with legacy `resource` / `ha.resource`, and new POST configurations fail before the parent purchase.
2. Allocation HCL is the complete authoritative namespace set. Missing namespaces are created, undeclared namespaces are removed, and reconciliation never deletes the last namespace. Namespace CU type is derived from the Workspace's pure single-zone or cross-zone mode and is retained through Read, import, and topology hashing.
3. Every namespace implicitly owns `default-queue`, with request equal to namespace fixed CU and limit equal to namespace maximum CU. A custom queue causes fail-closed behavior before the first cloud write. Internal capacity values use half-CU units for lossless observations, while the user-facing Workspace and namespace contract accepts integer CU only.
4. `implicit_topology_hash` is a sorted canonical `v1:<sha256>` over namespace CU type and every queue name/request/limit. It excludes used/status and other runtime fields, uses `schema.HashResource` for the namespace set, and is computed when all desired inputs are known.
5. Allocation Delete only detaches Terraform state with `SetId("")`; it performs no Workspace, namespace, or queue API call. Import accepts only the Workspace ID, sets both ID fields, performs no reconciliation, and lets the first Read import actual capacity, namespaces, queue hash, and observed tree.
6. Namespace Create uses the cws typed boundary: it copies the request, supplies explicit Workspace ID, maps cross-zone state, and writes initial `1 CU / 4 GiB` `ResourceSpec`. Only `FlinkPostCreateReadError` denotes Create/read ambiguity; string or HTTP-fingerprint inference is not used.
7. The unpublished `alicloud_flink_capacity_coordinator`, `capacity_management`, and `bootstrap_capacity` handshakes were removed without a stub or state upgrader. Unknown private coordinator state requires manual migration: record Workspace ID and complete absolute capacity, use the old provider to `state rm`, upgrade, declare allocation, import the Workspace ID, review the complete namespace set, then apply.
8. Legacy Workspace callbacks use an explicit readiness selection. Paid parent Create preserves its post-ID success boundary; the namespaces data source waits only for the service-created default namespace/queue, while child namespace Create validates only its exact target. Retry classification is typed and transport-aware; free-text error matching is not used.
9. Capacity reads validate non-nil, non-empty, exact Workspace identity at the common service boundary. A successful response with an empty or different `InstanceId` fails closed without List fallback and before any downstream namespace/queue access or capacity mutation.

## Implementation Notes

- Legacy Workspaces without `initial_capacity` retain their existing `resource` / `ha.resource` schema, ForceNew behavior, Read/CRUD, import, and ten xuanji legacy-leaf no-diff target.
- Allocation-managed namespaces and queues must not be managed simultaneously by legacy child resources.
- Reconciliation persists the last actual tree before an error, uses authoritative Read/replan after each cloud write, and exposes a diagnostic-only sorted `observed_capacity_tree`.
- The historical `903038` probe established that omitting `ResourceSpec` produces an invalid zero allocation even with headroom. Task D resolves that provider/cws-lib boundary in code; the combined-provider live recovery and zero-residual cleanup have completed.
- The production-shaped legacy runtime fixture invokes the real provider registry and production callbacks through OpenTofu plugin RPC. Its fake boundary is limited to the remote service/topology and unrelated Read/Delete callbacks. Its shared test ledger uses a stable sidecar `flock` so a canceled provider process cannot lose concurrent read-modify-write updates.

## Future Evolution Suggestions

- Complete the separately authorized canonical iac-env deployment gate with the reviewed same-version binary. Do not reuse local saved plans across OpenTofu versions.
- Preserve the isolated-mirror and installed-executable SHA gates whenever the `1.247.0` binary changes; rerun the three `cws-lib-iac` targets and all ten legacy leaves for each new binary SHA.
- Keep any future queue-management expansion separate from this implicit-default-queue contract and preserve the one-owner boundary.

## Related Files

- Feature Index: `.specify/memory/features.md`
- Design: `docs/superpowers/specs/2026-07-16-flink-workspace-capacity-allocation-design.md`
- Plan: `docs/superpowers/plans/2026-07-16-flink-workspace-capacity-allocation.md`
- Integration ledger: `.superpowers/sdd/progress.md`
- Allocation resource: `alicloud/resource_alicloud_flink_workspace_capacity_allocation.go`
- Capacity adapter: `alicloud/service_alicloud_flink_capacity.go`
