# Contracts: OSS Bucket Prune Before Delete

No new external API endpoints are introduced. This feature updates internal provider
behavior and uses existing OSS service APIs via the CWS-Lib-Go wrapper.
