# API Contracts

No new external API endpoints are created for this feature.
The interface is the Terraform Resource Schema for `alicloud_oss_bucket`.

## Schema Change

No schema attributes are added or changed. The behavior of `force_destroy` is enhanced implementation-side.
